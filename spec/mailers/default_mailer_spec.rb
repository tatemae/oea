require "spec_helper"

describe DefaultMailer do
  pending "Get EmailSpec active"
  # include EmailSpec::Helpers
  # include EmailSpec::Matchers
  # before(:all) do
  #   @user = FactoryGirl.create(:user)
  # end

  # subject { DefaultMailer.activate_account(@user) }

  # it { should deliver_from "Open Tapestry <#{ENV['support_email']}>" }
  # it { should deliver_to @user.email }
  # it { should have_subject Regexp.new("Thanks for your inquiry from #{ENV['application_name']}!") }
end
