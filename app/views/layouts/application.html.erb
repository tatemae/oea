<!DOCTYPE html>
<html>
<head>
  <title><%= yield(:page_title).presence || "Open Assessments" %></title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container-narrow">
    <div class="masthead">
      <%= render 'layouts/global/user_header' %>
    </div>
    <%= render 'layouts/global/messages' %>
    <div class="container">
      <%= yield %>
    </div>
  </div>
</body>
</html>